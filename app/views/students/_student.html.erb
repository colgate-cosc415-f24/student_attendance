<tr>
  <td><%= link_to student.name, student_path(student) %>
  <td><%= student.homeroom %>
  <td><%= student.graduation_year %>
  <% if current_user && !current_user.parent? %>
    <td><%= student.unexcused_absences %>
    <td><%= student.unexcused_tardy %>
  <% end %>
</tr>
